#include<stdio.h>
int main()
{
    int a;
    float b;
    char c;
    double d;
    printf("sizeof a is=%d\n",sizeof(a));
    printf("sizeof b is=%f\n",sizeof(b));
    printf("sizeof c is=%c\n",sizeof(c));
    printf("sizeof d is=%d\n",sizeof(d));
    return 0;
}